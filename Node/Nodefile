# Modern Build & Development Tools
npm install -g gulp

# Testing Frameworks
npm install -g karma
npm install -g mocha
npm install -g nyc  # Code coverage

# Development Tools
npm install -g eslint
npm install -g nodemon
npm install -g http-server

# Project Generators (modern/active ones)
npm install -g yo
npm install -g generator-jhipster
npm install -g generator-reveal
